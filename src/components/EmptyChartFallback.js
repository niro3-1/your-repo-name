// Fallback component for empty chart data
import React from 'react';

const EmptyChartFallback = () => {
    return <div>No data available for this chart.</div>;
};

export default EmptyChartFallback;